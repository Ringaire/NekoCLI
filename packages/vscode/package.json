{
  "name": "@nekocode/vscode",
  "version": "0.1.0",
  "description": "NekoCode VS Code extension",
  "type": "commonjs",
  "main": "./dist/extension.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "package": "vsce package"
  },
  "engines": {
    "vscode": "^1.90.0"
  },
  "devDependencies": {
    "@types/vscode": "^1.90.0",
    "typescript": "^5.7.0"
  }
}
